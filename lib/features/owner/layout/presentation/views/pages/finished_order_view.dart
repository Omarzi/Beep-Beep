import 'package:flutter/material.dart';

class FinishedOrdersView extends StatelessWidget {
  const FinishedOrdersView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
